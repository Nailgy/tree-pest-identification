# Stage 1 — Fruit-Tree Leaf Detector (YOLO11m)
# Target: Python 3.11 (newer versions cause dependency/build issues with this stack)
#
# Install order matters — the CUDA torch wheel must come from PyTorch's index:
#   1) py -3.11 -m venv venv  &&  venv\Scripts\activate   (Windows)
#   2) pip install torch==2.5.1 torchvision==0.20.1 --index-url https://download.pytorch.org/whl/cu124
#   3) pip install -r requirements.txt
#
# torch/torchvision are intentionally NOT pinned here: they ship from the cu124
# index above, not PyPI, so listing them in this file would pull CPU-only wheels.

ultralytics==8.3.40
sahi==0.11.18               # Stage 2 — sliced inference on 4K images (wraps the YOLO model)
albumentations==1.4.20      # enables Ultralytics' extra online augs (blur / CLAHE / grayscale)
opencv-python==4.9.0.80     # top of the overlap: ultralytics needs >=4.6.0, sahi needs <=4.9.0.80
PyYAML==6.0.2
numpy==1.26.4               # keep <2 for the torch 2.5.1 stack
pandas==2.2.3               # pin to tested version — pandas 3.x conflicts with numpy<2
matplotlib==3.9.2           # pin to tested version (ultralytics plotting); avoids latest-version drift
